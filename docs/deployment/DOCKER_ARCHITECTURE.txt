                    🚀 EHR Application - Docker Architecture
    
┌─────────────────────────────────────────────────────────────────────────┐
│                          Your Computer                                  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                    🐳 Docker Environment                        │   │
│  │                                                                 │   │
│  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │   │
│  │  │   Frontend   │    │   Backend    │    │  PostgreSQL  │    │   │
│  │  │   (React)    │◄───┤   (FastAPI)  │◄───┤   Database   │    │   │
│  │  │   Port 3000  │    │   Port 8000  │    │   Port 5433  │    │   │
│  │  └──────────────┘    └──────────────┘    └──────────────┘    │   │
│  │         │                    │                    │            │   │
│  │         │                    │                    │            │   │
│  │         │                    │                    │            │   │
│  │         └────────────────────┼────────────────────┘            │   │
│  │                              │                                 │   │
│  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │   │
│  │  │     OHIF     │    │   Orthanc    │    │  Orthanc DB  │    │   │
│  │  │    Viewer    │◄───┤     PACS     │◄───┤  PostgreSQL  │    │   │
│  │  │   Port 3001  │    │   Port 8042  │    │   Port 5434  │    │   │
│  │  └──────────────┘    └──────────────┘    └──────────────┘    │   │
│  │                                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                    📁 Your Local Files                           │  │
│  │                                                                  │  │
│  │  /home/linuxdev1/PracticeApps/EHRApp/                           │  │
│  │  ├── backend/          ◄──── Edit code here                     │  │
│  │  ├── frontend/         ◄──── Changes auto-reload!               │  │
│  │  ├── docker-compose.full.yml                                    │  │
│  │  └── docker-deploy.sh  ◄──── Run this to deploy                 │  │
│  │                                                                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                        How Code Changes Work

    You Edit Local File          Docker Detects Change          Auto-Reload
          (IDE)                        (Volume Mount)              (Container)
    
    backend/app/main.py   ──────►   /app/main.py    ──────►   uvicorn --reload
         Save file                    Same file!              Backend restarts!
    
    frontend/src/App.tsx  ──────►   /app/src/App.tsx ──────►   Vite HMR
         Save file                    Same file!              Browser updates!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                    🌍 Portability - Moving to New Machine

    ┌─────────────────────────────────────────────────────────────┐
    │                     Machine A (Developer 1)                  │
    │                                                              │
    │    git push  ──────────────────────┐                        │
    │                                     │                        │
    └─────────────────────────────────────┼────────────────────────┘
                                          │
                                          ▼
                            ┌─────────────────────────┐
                            │     Git Repository      │
                            │   (GitHub/GitLab)       │
                            └─────────────────────────┘
                                          │
                                          ▼
    ┌─────────────────────────────────────┼────────────────────────┐
    │                                     │                        │
    │    git clone ◄──────────────────────┘                        │
    │    ./docker-deploy.sh                                        │
    │                                                              │
    │                     Machine B (Developer 2)                  │
    │              Identical environment! Same versions!           │
    └──────────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                        Development Workflow

    Start Once              Edit Anytime            Test Continuously
    
    ./docker-deploy.sh      Edit .py files          http://localhost:3000
           │                Edit .tsx files         Changes appear instantly!
           │                Save files                       │
           │                     │                           │
           ▼                     ▼                           ▼
    All services start    Docker auto-reloads      Test in browser
    (takes 5-10 min)      (instant!)               (F5 refresh)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                    Common Commands Quick Reference

    # Start everything
    ./docker-deploy.sh

    # View logs
    docker-compose -f docker-compose.full.yml logs -f

    # Stop everything
    docker-compose -f docker-compose.full.yml down

    # Restart specific service
    docker-compose -f docker-compose.full.yml restart backend

    # Run migrations
    docker exec -it ehr_backend alembic upgrade head

    # Access database
    docker exec -it ehr_postgres psql -U ehr_user -d ehr_db

    # Complete reset (emergency)
    docker-compose -f docker-compose.full.yml down -v
    ./docker-deploy.sh


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                    Benefits vs Traditional Setup

    Traditional Setup:
    ❌ Install Python 3.11
    ❌ Create virtual environment
    ❌ Install Node.js 18
    ❌ Install npm packages
    ❌ Configure PostgreSQL
    ❌ Configure environment variables
    ❌ Run database migrations
    ❌ Start backend manually
    ❌ Start frontend manually
    ❌ Different on each machine
    ⏱️  Takes: 1-2 hours

    Docker Setup:
    ✅ Install Docker Desktop (one-time)
    ✅ Run: ./docker-deploy.sh
    ✅ Wait 5-10 minutes
    ✅ Everything running!
    ✅ Same on every machine
    ⏱️  Takes: 10 minutes


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For complete documentation see:
  • SETUP_GUIDE_FOR_NEW_MACHINE.md - Beginner-friendly setup guide
  • ONGOING_DEVELOPMENT.md - Continue development with Docker
  • QUICK_REFERENCE.md - Command cheat sheet
  • DOCKER_DEPLOYMENT.md - Technical details
